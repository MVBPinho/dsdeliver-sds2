export type Product = {
    id: number;
    name: string;
    price: number;
    drescription: string;
    imageUri: string;
}

export type OrderLocationdata = {
    latitude: number;
    longitude: number;
    address: string;
}